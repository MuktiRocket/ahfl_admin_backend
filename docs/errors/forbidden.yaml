description: Unauthorized request. The client does not have access rights to the content.
content:
  application/json:
    schema:
      type: object
      required:
        - type
        - httpStatus
        - description
      properties:
        type: 
          type: string
        httpStatus:
          type: integer
          example: 403
        message:
          type: string
        additionalInfo:
          type: object