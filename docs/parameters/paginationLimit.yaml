in: query
name: limit
required: false
schema:
  type: integer
  minimum: 0
  maximum: 100
  default: 20
  example: 20
  description: The limit parameter for pagination. No more than this number of entities will be returned, but less may be returned.
